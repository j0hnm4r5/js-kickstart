/**
 * @file Root file for server-side application.
 */

// ========= LIBS =========
import hagen from "hagen";

// ========= COMPONENTS =========
import "./components/sockets";

hagen.log(`APP`, `Starting App`);
